# Telegram bot uchun
python-telegram-bot==20.3

# Asinxron web server (Flask o‘rniga ishlatiladi)
quart==0.19.6

# Asinxron HTTP client (webhook URL set qilish uchun)
aiohttp==3.9.5

# Production serverda ishlatish uchun
uvicorn==0.30.1
gunicorn==22.0.0
waitress==3.0.0

# Qo‘shimcha qulaylik uchun (environment o‘zgaruvchilarni boshqarish)
python-dotenv==1.0.1

# Oddiy HTTP so‘rovlar uchun (requests ham qoldirildi)
requests==2.32.3
httpx==0.24.1